name: Sultan-xxKSU-Build

permissions:
  contents: write
  actions: write

on:
  workflow_dispatch:
  push:
    paths-ignore:
      - 'README.md'

jobs:
  build:
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        # This will trigger two parallel builds: one with and one without SUSFS
        susfs: [false, true]
        build:
          - type: xxKSU
            repo: "backslashxx/KernelSU"
            folder: "KernelSU"
            branch: "master"
            ak3_suffix: "_xxKSU"
        kernel:
          - codename: gs201
            repo: "android_kernel_google_tensynos"
            android_version: "android14"
            kernel_version: "6.1"
            defconfig: gs201_defconfig
            local_version: "-android14-11-gc1de4747ac59-ab14219743"
            build_date: "Mon Oct  6 16:50:48 UTC 2025"

    steps:
      - name: Download and extract GCC 14.2.0 toolchain
        run: |
          wget https://www.kernel.org/pub/tools/crosstool/files/bin/x86_64/14.2.0/x86_64-gcc-14.2.0-nolibc-aarch64-linux.tar.gz -O gcc.tar.gz
          gunzip gcc.tar.gz
          tar -xf gcc.tar
          echo "TOOLCHAIN_PATH=$GITHUB_WORKSPACE/gcc-14.2.0-nolibc/aarch64-linux/bin/aarch64-linux-" >> $GITHUB_ENV

      - name: Clone Dependencies and set ENV
        run: |
          ANYKERNEL_BRANCH="sultan-${{ matrix.kernel.codename }}"
          SUSFS_BRANCH="gki-${{ matrix.kernel.android_version }}-${{ matrix.kernel.kernel_version }}"

          sudo apt-get install -y --no-install-recommends ccache
          
          git clone --depth=1 https://github.com/TheWildJames/AnyKernel3.git -b "$ANYKERNEL_BRANCH" AK3
          echo "ak3_dir=$PWD/AK3" >> $GITHUB_ENV

          git clone --depth=1 https://github.com/yapixel/kernel_patches.git -b dev my_patches
          echo "patch_dir=$PWD/my_patches" >> $GITHUB_ENV

          if [[ "${{ matrix.susfs }}" == "true" ]]; then
            git clone --depth=5 https://gitlab.com/simonpunk/susfs4ksu.git -b "$SUSFS_BRANCH" susfs
            echo "susfs_dir=$PWD/susfs" >> $GITHUB_ENV
          fi
          
          git clone --depth=1 https://github.com/kerneltoast/${{ matrix.kernel.repo }} sultan
          echo "kernel_dir=$PWD/sultan" >> $GITHUB_ENV

          build_type="${{ matrix.build.ak3_suffix }}"
          [[ "${{ matrix.susfs }}" == "true" ]] && build_type="${build_type}-SUSFS" || build_type="${build_type}-NoSUSFS"
          echo "zip_name=$(date +"%d.%m.%Y")-${{ matrix.kernel.codename }}${build_type}-Sultan" >> $GITHUB_ENV

      - name: Setup Base Kernel (Hooks & Configs)
        run: |
          cd "${{ env.kernel_dir }}"
          
          # Patch syscall hooks for BOTH versions (SUSFS and No-SUSFS)
          patch -p1 -F 3 < "${{ env.patch_dir }}/syscall_hook/scope-min-hooks-v1.9.patch"

          # Basic KSU configs
          configs="CONFIG_KSU=y CONFIG_TMPFS_XATTR=y CONFIG_TMPFS_POSIX_ACL=y"
          
          # Only add SUSFS configs if applicable
          if [[ "${{ matrix.susfs }}" == "true" ]]; then
            configs="$configs CONFIG_KSU_SUSFS=y CONFIG_KSU_SUSFS_SUS_PATH=y CONFIG_KSU_SUSFS_SUS_MOUNT=y CONFIG_KSU_SUSFS_SUS_KSTAT=y CONFIG_KSU_SUSFS_TRY_UMOUNT=y CONFIG_KSU_SUSFS_SPOOF_UNAME=y"
          fi

          for config in $configs; do
            echo -e "\n${config}" >> "arch/arm64/configs/${{ matrix.kernel.defconfig }}"
          done

      - name: Sync xxKSU Source
        run: |
          cd "${{ env.kernel_dir }}"
          # Creates the KernelSU folder for both builds
          curl -LSs "https://raw.githubusercontent.com/${{ matrix.build.repo }}/${{ matrix.build.branch }}/kernel/setup.sh" | bash -s ${{ matrix.build.branch }}

      - name: Apply SUSFS-Specific Patches
        if: matrix.susfs
        run: |
          # 1. Copy core SUSFS headers/files
          cp ${{ env.susfs_dir }}/kernel_patches/fs/* ${{ env.kernel_dir }}/fs/
          cp ${{ env.susfs_dir }}/kernel_patches/include/linux/* ${{ env.kernel_dir }}/include/linux/

          # 2. Patch the newly synced xxKSU source
          cd "${{ env.kernel_dir }}/${{ matrix.build.folder }}"
          patch -p1 < "${{ env.patch_dir }}/xxksu/0001-xxKSU-kernel-implement-susfs-v2.1.0-De-inlined.patch"
          
          # 3. Patch the main kernel tree
          cd "${{ env.kernel_dir }}"
          patch -p1 < ${{ env.susfs_dir }}/kernel_patches/50_add_susfs_in_gki-${{ matrix.kernel.android_version }}-${{ matrix.kernel.kernel_version }}.patch || true
          
          # 4. Final Sultan/SUSFS fix
          patch -p1 < "${{ env.patch_dir }}/sultan/sys.c_fix.patch"

      - name: Build Kernel
        run: |
          cd "${{ env.kernel_dir }}"
          export KBUILD_BUILD_USER="TheSillyOk"
          export KBUILD_BUILD_HOST="Github"
          
          # Generate defconfig and Compile
          make CROSS_COMPILE=${{ env.TOOLCHAIN_PATH }} CC="${{ env.TOOLCHAIN_PATH }}gcc" -j$(nproc --all) ${{ matrix.kernel.defconfig }}
          make CROSS_COMPILE=${{ env.TOOLCHAIN_PATH }} CC="${{ env.TOOLCHAIN_PATH }}gcc" -j$(nproc --all)

      - name: Package AnyKernel3
        run: |
          cp ${{ env.kernel_dir }}/out/arch/arm64/boot/Image.lz4 ${{ env.ak3_dir }}/Image.lz4
          cat ${{ env.kernel_dir }}/out/google-devices/${{ matrix.kernel.codename }}/dts/*.dtb > ${{ env.ak3_dir }}/dtb

      - name: Upload Artifact
        uses: actions/upload-artifact@v4
        with:
          name: ${{ env.zip_name }}
          path: ${{ env.ak3_dir }}/*
