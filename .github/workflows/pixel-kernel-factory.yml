name: Build Unified Pixel Kernel — Sultan Matrix

on:
  workflow_dispatch:

env:
  ANYKERNEL_REPO: https://github.com/TheWildJames/AnyKernel3.git
  PATCHES_REPO: https://github.com/yapixel/kernel_patches
  SCRIPTS_REPO: https://github.com/TheSillyOk/sh
  SUSFS_REPO: https://gitlab.com/simonpunk/susfs4ksu
  GCC_VERSION: "14.2.0"
  GCC_ARCHIVE_URL: "https://kernel.org/pub/tools/crosstool/files/bin/x86_64/14.2.0/x86_64-gcc-14.2.0-nolibc-aarch64-linux.tar.gz"
  KBUILD_BUILD_USER: "build-user"
  KBUILD_BUILD_HOST: "build-host"

jobs:
  build:
    name: Build ${{ matrix.codename }} (${{ matrix.variant_name  }})
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        include:
          # --- Category I: xxKSU-SUSFS (Anaconda + Trijal08 KSU) ---
          - codename: zuma
            variant_name: "xxKSU-SUSFS"
            kernel_repo: "https://github.com/Trijal08/anaconda_kernel_google_tensynos"
            kernel_branch: "anaconda-tensynos-qpr3"
            ksu_repo: "https://github.com/Trijal08/KernelSU"
            ksu_branch: "master-susfs"
            use_susfs: true
            dtb_path: "google-devices/zuma/dts"
            defconfig: zuma_defconfig

          # --- Category II: KowSU-SUSFS (Official Sultan + KOWX712) ---
          - codename: zuma
            variant_name: "KowSU-SUSFS"
            kernel_repo: "https://github.com/kerneltoast/android_kernel_google_tensynos"
            kernel_branch: "16.0.0-sultan"
            ksu_repo: "https://github.com/KOWX712/KernelSU"
            ksu_branch: "master"
            use_susfs: true
            dtb_path: "google-devices/zuma/dts"
            defconfig: zuma_defconfig

          # --- Category III: xxKSU (Official Sultan + backslashxx) ---
          - codename: zuma
            variant_name: "xxKSU"
            kernel_repo: "https://github.com/kerneltoast/android_kernel_google_tensynos"
            kernel_branch: "16.0.0-sultan"
            ksu_repo: "https://github.com/backslashxx/KernelSU"
            ksu_branch: "master"
            use_susfs: false
            dtb_path: "google-devices/zuma/dts"
            defconfig: zuma_defconfig

    steps:
      - uses: actions/checkout@v6

      - name: Setup Toolchain
        run: |
          wget -q "${{ env.GCC_ARCHIVE_URL }}" -O gcc.tar.gz
          tar -xf gcc.tar.gz
          echo "GCC_PATH=$PWD/gcc-${{ env.GCC_VERSION }}-nolibc/aarch64-linux/bin/aarch64-linux-" >> "$GITHUB_ENV"

      - name: Clone Components
        run: |
          git clone --depth=1 "${{ matrix.kernel_repo }}" -b "${{ matrix.kernel_branch }}" sultan
          git clone --depth=1 "${{ matrix.ksu_repo }}" -b "${{ matrix.ksu_branch }}" KernelSU
          git clone --depth=1 "${{ env.ANYKERNEL_REPO }}" -b "sultan-${{ matrix.codename }}" AK3
          git clone --depth=1 "${{ env.PATCHES_REPO }}" kernel_patches
          git clone --depth=1 "${{ env.SCRIPTS_REPO }}" sh
          if [ "${{ matrix.use_susfs }}" = "true" ]; then
            git clone --depth=1 "${{ env.SUSFS_REPO }}" -b "gki-android14-6.1" susfs4ksu
          fi

      - name: Integration & Patching
        run: |
          cd sultan
          # 1. Reproducibility Patches (60, 61, 62)
          patch -p1 < ../kernel_patches/sultan/60_scripts_mkcompile_h.patch
          patch -p1 < ../kernel_patches/sultan/61_scripts_setlocalversion.patch
          patch -p1 < ../kernel_patches/sultan/62_init_Makefile.patch
          
          # 2. Variant Specific Logic
          if [[ "${{ matrix.variant_name }}" == "KowSU-SUSFS" ]]; then
            # Patch KSU (10 Patch)
            cd ../KernelSU
            patch -p1 -l --fuzz=3 < ../kernel_patches/kowsu/10_enable_susfs_for_ksu.patch
            cd ../sultan
            # Patch Kernel (50 Patch + sys.c_fix)
            cp ../susfs4ksu/kernel_patches/50_add_susfs_in_gki-android14-6.1.patch ./
            cp -r ../susfs4ksu/kernel_patches/fs/* ./fs/
            cp -r ../susfs4ksu/kernel_patches/include/linux/* ./include/linux/
            patch -p1 --fuzz=10 < 50_add_susfs_in_gki-android14-6.1.patch
            patch -p1 --fuzz=3 < ../kernel_patches/sultan/sys.c_fix.patch
          fi

          # 3. Driver Linking
          ln -sf ../../KernelSU/kernel drivers/kernelsu
          sed -i '/endmenu/i source "drivers/kernelsu/Kconfig"' drivers/Kconfig
          echo 'obj-$(CONFIG_KSU) += kernelsu/' >> drivers/Makefile

      - name: Metadata Extraction
        id: meta
        run: |
          # Kernel ID (Tag or SHA)
          if [[ "${{ matrix.kernel_repo }}" == *"anaconda"* ]]; then
            S_TAG=$(git -C sultan rev-parse --short HEAD)
          else
            S_TAG=$(git -C sultan tag --sort=-committerdate | head -n1)
          fi
          
          # KSU Revision
          chmod +x ./sh/get_ksuver.sh
          KSU_ORG=$(echo "${{ matrix.ksu_repo }}" | cut -d'/' -f4)
          K_VER=$(./sh/get_ksuver.sh "$KSU_ORG" "KernelSU" "${{ matrix.ksu_branch }}" || echo "unknown")
          
          # SUSFS Version
          if [ "${{ matrix.use_susfs }}" = "true" ]; then
            S_VER=$(grep -m1 'define SUSFS_VERSION' sultan/include/linux/susfs.h | grep -oP '".*?"' | tr -d '"' || echo "integrated")
          else
            S_VER="none"
          fi

          echo "s_tag=$S_TAG" >> "$GITHUB_OUTPUT"
          echo "ksu_ver=$K_VER" >> "$GITHUB_OUTPUT"
          echo "susfs_ver=$S_VER" >> "$GITHUB_OUTPUT"

      - name: Compilation
        working-directory: sultan
        run: |
          export CROSS_COMPILE=${{ env.GCC_PATH }}
          export CC=${{ env.GCC_PATH }}gcc
          make O=out ${{ matrix.defconfig }}
          
          # Inject Configuration Pillars
          cat <<EOF >> out/.config
          CONFIG_KSU=y
          CONFIG_NETFILTER_ADVANCED=y
          CONFIG_IP_SET=y
          CONFIG_IP_SET_MAX=65534
          CONFIG_TCP_CONG_BBR=y
          CONFIG_DEFAULT_BBR=y
          CONFIG_NET_SCH_FQ=y
          CONFIG_XFRM_USER=y
          CONFIG_XFRM_SUB_POLICY=y
          CONFIG_TMPFS_XATTR=y
          CONFIG_TMPFS_POSIX_ACL=y
          CONFIG_WERROR=n
          EOF

          if [[ "${{ matrix.variant_name }}" == *"SUSFS" ]]; then
            cat <<EOF >> out/.config
          CONFIG_KSU_SUSFS=y
          CONFIG_KSU_SUSFS_SUS_PATH=y
          CONFIG_KSU_SUSFS_SUS_MOUNT=y
          CONFIG_KSU_SUSFS_SPOOF_UNAME=y
          CONFIG_KSU_SUSFS_OPEN_REDIRECT=y
          EOF
          fi

          if [[ "${{ matrix.variant_name }}" == "xxKSU" ]]; then
            cat <<EOF >> out/.config
          CONFIG_KSU_TAMPER_SYSCALL_TABLE=y
          CONFIG_KSU_LSM_SECURITY_HOOKS=y
          CONFIG_KSU_THRONE_TRACKER_ALWAYS_THREADED=y
          CONFIG_KSU_KPROBES_HOOK=n
          EOF
          fi

          make O=out CROSS_COMPILE="$CROSS_COMPILE" CC="$CC" olddefconfig
          make O=out CROSS_COMPILE="$CROSS_COMPILE" CC="$CC" -j"$(nproc --all)"

      - name: Packaging
        run: |
          cp sultan/out/arch/arm64/boot/Image.lz4 AK3/Image.lz4 || cp sultan/out/arch/arm64/boot/Image AK3/Image
          cat sultan/out/${{ matrix.dtb_path }}/*.dtb > AK3/dtb
          
          STAG="${{ steps.meta.outputs.s_tag }}"
          KVER="${{ steps.meta.outputs.ksu_ver }}"
          SVER="${{ steps.meta.outputs.susfs_ver }}"
          
          if [[ "${{ matrix.variant_name }}" == "xxKSU" ]]; then
            ZIP_NAME="Sultan-${{ matrix.codename }}-${STAG}-xxKSU-r${KVER}.zip"
          else
            ZIP_NAME="Sultan-${{ matrix.codename }}-${STAG}-${{ matrix.variant_name }}-r${KVER}-SUSFS-${SVER}.zip"
          fi
          
          cd AK3 && zip -r9 "../${ZIP_NAME}" . -x "*.git*"
          echo "ZIP_NAME=${ZIP_NAME}" >> "$GITHUB_ENV"

      - name: Upload
        uses: actions/upload-artifact@v4
        with:
          name: ${{ matrix.variant_name }}-${{ matrix.codename }}
          path: ${{ env.ZIP_NAME }}


 release:
 needs: build
 runs-on: ubuntu-latest
 permissions: { contents: write }
 steps:
      - uses: actions/download-artifact@v4
        with: 
          path: ./artifacts
          merge-multiple: true
      
      - name: Create GitHub Release
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          # 1. Generate the Tag
          DATE_STR=$(date +"%y%m%d")
          # Check for existing tags to increment rX
          EXISTING_COUNT=$(gh api repos/${{ github.repository }}/tags --jq '.[].name' | grep "$DATE_STR" | wc -l || echo "0")
          TAG="${DATE_STR}-r$((EXISTING_COUNT + 1))"
          
          # 2. Extract info from the artifact filenames to build the body
          # We parse the first zip found to reconstruct the dashboard
          SAMPLE_ZIP=$(ls artifacts/*.zip | head -n 1 | xargs basename)
          
          # Logic to build a clean body without needing matrix variables
          cat <<EOF > body.md
          ### 🚀 Build Information
          | Component | Details |
          | :--- | :--- |
          | **Build Date** | $(date +"%Y-%m-%d") |
          | **Toolchain** | [GCC 14.2.0](https://kernel.org/pub/tools/crosstool/files/bin/x86_64/14.2.0/) |
          | **Status** | Stable / Unified |
          EOF
          
          # 3. Final Release Creation
          gh release create "$TAG" artifacts/*.zip --title "Kernel Build — $TAG" --notes-file body.md
          fi
          
          gh release create "$TAG" artifacts/*.zip --title "Kernel Build — $TAG" --notes-file body.md
